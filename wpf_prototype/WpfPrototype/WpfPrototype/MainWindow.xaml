<Window x:Class="WpfPrototype.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfPrototype"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel Orientation="Horizontal">
        <Button x:Name="btn1" Content="Button" HorizontalAlignment="Left" Height="52" VerticalAlignment="Top" Width="245" Click="Button_Click"/>
        <TextBox x:Name="txtBox1" HorizontalAlignment="Left" TextWrapping="Wrap" 
                 VerticalAlignment="Top" Width="120"
                 Text="{Binding MyProperty, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 />
        <Label x:Name="lbl1" HorizontalAlignment="Left"
               VerticalAlignment="Top" RenderTransformOrigin="1.652,0.162"
               Content="{Binding MyProperty}"
               />

    </StackPanel>
</Window>
